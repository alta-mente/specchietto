The base surface container — white, rounded (20px), soft shadow.

```jsx
<Card padding="md">…</Card>
<Card interactive onClick={open}>Tavolo 12</Card>
```
`interactive` adds a hover lift for clickable cards. `elevation` = flat | sm | md | lg.
