On/off toggle for settings and quick actions. Works controlled or uncontrolled.

```jsx
<Switch checked={on} onChange={e=>setOn(e.target.checked)} label="Conferma automatica" />
```
Use `size="sm"` in dense settings lists.
